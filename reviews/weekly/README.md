# 每周汇总 | Weekly Summaries

每周日聚合本周 `reviews/daily/*.md`，产出：

1. 本周主题与关键产出
2. **技能变更提案**（新增 / 修改 / 删除，含理由）
3. 待办跟进

用户确认后，技能改动按 `main拉分支→上传→提交→PR` 提交，并更新根目录 `CHANGELOG.md`。

命名：`YYYY-Www.md`（ISO 周，例如 `2026-W33.md`）。
